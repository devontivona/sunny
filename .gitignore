# Installed third-party reference skills (managed via `npx skills add`)
.agents/
.claude/skills/ai-sdk
.claude/skills/chat-sdk
.claude/skills/workflow
.claude/skills/agentation
.claude/skills/agentation-self-driving

# Node / build
node_modules/
dist/
build/
*.tsbuildinfo

# Environment & secrets — never commit
.env
.env.*
*.env
!.env.example
secrets/
*op-service-account*
*.token

# Runtime state (lives under ~/.sunny, never in the repo)
.sunny/

# Test/coverage output
coverage/

# OS / editor
.DS_Store
*.log
*.swp

# Nitro build output
.output/
.nitro/

# SWC compiler cache (workflow/nitro)
.swc/

# Eval run scorecards (artifacts; evals/baseline.json is committed)
evals/scorecards/

# Experiment result dumps (artifacts)
evals/experiments/results/
