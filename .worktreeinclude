# Files to copy into new git worktrees created by Claude Code.
# These are gitignored (not committed), so they don't exist in a fresh worktree
# unless listed here. Patterns are matched against the repo root.

.env
.env.local
.env.*.local
