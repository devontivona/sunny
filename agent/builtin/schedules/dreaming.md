---
cron: "30 */4 * * *"
audience: nobody
authority: memory_read, memory_write, bash, file_read, file_write
---
Dreaming (recurring memory maintenance): follow your dreaming skill — read its SKILL.md and execute the procedure exactly (digest via the sunny CLI, fold durable facts into memory, reconcile INDEX, write compaction summaries, advance the watermark). End with the one-line outcome the skill specifies.
