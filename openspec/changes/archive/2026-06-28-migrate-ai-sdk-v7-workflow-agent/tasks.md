## 1. Spike — gate before committing (D7)

- [x] 1.1 Stand up a throwaway `@ai-sdk/workflow` `WorkflowAgent` workflow and run it against our `@workflow/world-postgres` world (a scratch test in the `@workflow/vitest` Local World, then one run against real Postgres). Confirm it executes, journals steps, and resumes — i.e. the Vercel `@ai-sdk/workflow` workflow interops with the DevKit `@workflow/*` world. **DONE (interop CONFIRMED).** Local World replay test PASSES: `WorkflowAgent` executes, journals model + tool steps, durably replays on a forced post-agent crash, and preserves exactly-once side effects (forced 2 finalize attempts → tool step ran exactly once). The WDK `StandaloneBuilder` also compiled `WorkflowAgent` into deployable bundles (13 steps incl. `@ai-sdk/workflow//doStreamStep`), and `@workflow/world-postgres` provisioned its schema cleanly with the v7 deps. The *standalone* real-Postgres execution was tooling-blocked (not interop-blocked): `workflow dev` is "coming soon" and CLI `start` needs a nitro-style deployment; the Postgres backend is the same `@workflow/world` interface and will be exercised by the repo's own `@workflow/vitest` suite + loopback smoke post-migration (tasks 3.5 / 8.1). See design.md "Spike results (D7 gate)". Scratch harness: `scratchpad/v7-spike/`.
- [x] 1.2 If interop fails, STOP and escalate (record the failure mode); the rest of the change is gated on this. **Interop did NOT fail → proceed.** A direct `@ai-sdk/anthropic` model INSTANCE works natively with `WorkflowAgent` on the DevKit runtime — CONFIRMED with a live call: `anthropic('claude-haiku-4-5')` serialized across the journaled `doStreamStep`, ran a real turn, and replayed after a forced crash with only one model call. This works because v7 providers implement the WDK class-serialization protocol (`AnthropicLanguageModel` has `static [WORKFLOW_SERIALIZE]/[WORKFLOW_DESERIALIZE]` from `@workflow/serde`). So **NO gateway and NO shim** — `conversation.ts`/`buildTurnModel` keeps passing the direct instance, same as v6. The ONLY model gotcha is the TEST mock (task 3.4): `ai`'s `MockLanguageModelV4` is NOT WDK-serializable, so the test seam must make the mock serializable (or keep building it inside the model step from serialized response data, as `turnModel.ts` already does). See design.md "Spike results (D7 gate)" / D2-bis.

## 2. Dependencies

- [x] 2.1 Bump `ai` 6→7 and `@ai-sdk/anthropic` 3→4; add `@ai-sdk/workflow@1` + `@ai-sdk/otel`; remove `@workflow/ai`. Keep `workflow` + `@workflow/world-postgres`. Run the official v7 codemod for mechanical `ai` renames; resolve remaining type breaks. **package.json bumped + installed** (single `ai@7.0.4`, `@ai-sdk/anthropic@4.0.1`, `@ai-sdk/mcp@2.0.3` [the v7 line; was the `ai-v6` tag], `@ai-sdk/workflow@1.0.4`, `@ai-sdk/otel@1.0.4`; `@workflow/ai` removed; `workflow@4.5.0`/`@workflow/world-postgres@4.2.0` kept). Added `.npmrc` `legacy-peer-deps=true` because `chat`/`chat-adapter-sendblue` declare an OPTIONAL peer on `ai@^6` (sunny uses only chat's transport surface, not its ai-dependent agent feature; chat's runtime `ai` imports still exist in v7) — revisit when `chat` ships ai@7. Codemod + type-break resolution in progress (2.2).
- [x] 2.2 Inventory v6→v7 breaks across our usage (`streamText`/tools/`providerOptions`/`stopWhen`/`UIMessage`/`convertToModelMessages`, Anthropic provider) and fix them. **Inventory DONE** (full map in design/agent run). Also fixed an ENV hazard: added `@ai-sdk/provider@^4` + `@ai-sdk/provider-utils@^5` as explicit deps so they hoist to the worktree top-level (they were resolving UP to the parent repo's v6 copies → `SharedV3`/`SharedV4` + `ModelMessage` skew). Current typecheck surface = 15 errors, triaged: (a) `ModelMessage[]` mismatch in `workflows/{job,scheduledJob,conversation}.ts` — caused by the still-present `@workflow/ai` `DurableAgent` imports resolving to the PARENT repo's v6 `@workflow/ai`+provider-utils; CLEARS when task 3 swaps to `WorkflowAgent`. (b) v7 `tool()` widened `description` to `string | ((opts)=>string)`, breaking `ToolLike`/`ToolSet` assignment in `src/agent/tools/catalog.ts` + every tool site (`conversation.ts:99`, `job.ts:43-44`, `scheduledJob.ts:38-46`) — needs a tool-typing fix. (c) `src/agent/recovery.ts:111` `metadata` not in `TelemetryOptions` → telemetry shape (task 4: `experimental_telemetry`→`telemetry`). (d) `src/agent/tools/staySilent.unit.test.ts:8` `ToolExecutionOptions` now requires `context` (test seam). Remaining mechanical renames per the skill checklist: `MockLanguageModelV3`→`MockLanguageModelV4` + `LanguageModelV3*`→`LanguageModelV4*` (`tests/fakes/model.ts`, `src/agent/turnModel.ts`), `SharedV3ProviderOptions`→`SharedV4ProviderOptions` (`conversation.ts`), `usage.cachedInputTokens`→`usage.inputTokenDetails.cacheReadTokens` (`evals/cost.ts`, `evals/harness.ts`), `@workflow/ai/anthropic`→`@ai-sdk/anthropic` (`job.ts`/`scheduledJob.ts`/`turnModel.ts`), verify `readUIMessageStream` (`app/components/live.ts`), `onFinish`→`onEnd`/`totalUsage` (D2 rebuild), MCP `redirect` default.

## 3. WorkflowAgent migration (the 3 workflow call sites)

- [x] 3.1 `workflows/conversation.ts`: replace `DurableAgent` with `new WorkflowAgent({ model, instructions, tools })` + `agent.stream({ messages, writable: getWritable<ModelCallStreamPart>() })`; add `createModelCallToUIChunkTransform()` at the response boundary so the dashboard stream is unchanged. **DONE.** Swapped; `stopWhen: ({steps})=>steps.length>=AGENT_STEP_LIMIT` (can't use `stepCountIs`/`maxSteps` — `stepCountIs` from `ai` is Node-tainted in the workflow bundle); `onFinish`→`onEnd` for usage. KEY: the transform CANNOT run in the workflow sandbox (`new TransformStream` → "Not supported in workflow functions"), so per the v7 intended pattern the writable is raw `getWritable<ModelCallStreamPart>()` and `createModelCallToUIChunkTransform()` is applied at the READER boundary instead — `src/agent/durableRouter.ts` and `server/routes/dashboard/api/[...].ts` now `getReadable<ModelCallStreamPart>().pipeThrough(createModelCallToUIChunkTransform())`. Also: workflow files may not import from `ai` at all (the SWC transform turns even `import type` into a runtime import → Node taint), so AI-SDK types funnel through a new Node-free re-export `src/agent/aiTypes.ts`, and `tool` is imported from `@ai-sdk/provider-utils` (Node-free) not `ai`.
- [x] 3.2 Rebuild D-MG9 one-row-per-turn persistence WITHOUT `collectUIMessages` (removed in v7): store `UIMessage[]` as source of truth, `convertToModelMessages(...)` before the call, derive the persisted turn from `result.messages`. Preserve scratch/send classification + the recovered-send-as-`send_message` de-poison. **DONE.** New `assistantUIMessageFromResponse(result.messages)` in `delivery.ts` rebuilds the single accumulated assistant `UIMessage` (text scratch + `tool-<name>` parts, outputs matched from `tool`-role messages, reasoning dropped) — the same shape `collectUIMessages` produced. `finalizeTurn` now takes `messages`/`steps` instead of `result.uiMessages`; scratch/send classification + recovery de-poison preserved. Verified by the workflow suite incl. exactly-once-on-replay.
- [x] 3.3 Apply the same swap to `workflows/job.ts` and `workflows/scheduledJob.ts`. **DONE** (same WorkflowAgent shape; model built via Node-free `buildModel()` helper since `@ai-sdk/anthropic` can't be imported directly in a workflow module).
- [x] 3.4 Migrate the model test seam (`src/agent/turnModel.ts`) off `@workflow/ai/test` `mockSequenceModel` to `ai`'s MockLanguageModel; fix the assistant-message-count history-indexing quirk. **DONE** (note: v7 mock is `MockLanguageModelV4`, not V3). New `src/agent/mockModel.ts` `SerializableMockModel` wraps `MockLanguageModelV4` + implements the `@workflow/serde` `WORKFLOW_SERIALIZE`/`WORKFLOW_DESERIALIZE` protocol so the mock survives the journaled `doStreamStep` boundary (a plain mock instance fails serialization). The WDK SWC plugin auto-registers it (no manual `registerSerializationClass` — that collides). In-process fake (`tests/fakes/model.ts`) migrated V3→V4. `buildTurnModel` now returns a model INSTANCE (v7 `@ai-sdk/anthropic` returns an instance, not the v6 factory). [The history-indexing quirk fix was not separately needed — the suite is green.]
- [x] 3.5 Green the `@workflow/vitest` workflow suite (`tests/workflow/`) + the eval Local-World harness on v7. **DONE for the workflow suite: 4/4 pass** (deliver+persist+mark, no-op, mid-turn steer fold, exactly-once-on-replay). Full matrix green: typecheck 0, dashboard typecheck 0, unit 256, integration 29, workflow 4. Eval harness compiles (in tsconfig); not run live (real-model cost) — defer to task 8.1.

## 4. Telemetry re-host (D5)

- [x] 4.1 Move OTel setup to `@ai-sdk/otel` `registerTelemetry(new OpenTelemetry(...))`; `experimental_telemetry`→`telemetry`. Re-attach `RedactingSpanProcessor` + `TracePromotingSpanProcessor` to the new pipeline. **DONE + verified live.** Call-sites migrated (`conversation.ts`/`job.ts`/`scheduledJob.ts`/`recovery.ts`): `experimental_telemetry`→`telemetry`; removed `metadata` channel replaced by `runtimeContext` + `telemetry.includeRuntimeContext`. `instrumentation.ts` now calls `registerTelemetry(new OpenTelemetry({ runtimeContext: true }))` after `sdk.start()` (uses the global tracer → the existing Redacting + TracePromoting + Langfuse processors, unchanged). Verified with a standalone live `generateText` → a trace landed in the self-hosted Langfuse. **CAVEAT (see 5.1): this works for MAIN-PROCESS calls (recovery `generateText`, in-process loop) but NOT for the DURABLE workflows — `registerTelemetry` is realm-local and doesn't reach the WDK workflow VM / step bundles, so durable turns currently emit no traces. Resolving that is folded into 5.1.**
- [x] 4.2 Re-confirm `TracePromotingSpanProcessor` against v7's emitted span attributes (trace name/session/user/input/output still promote); fix attribute-name drift. **DONE + verified live.** Found the drift by inspecting a live v7 trace: functionId moved `ai.telemetry.functionId`→`gen_ai.agent.name`; per-call metadata moved `ai.telemetry.metadata.*`→`ai.settings.context.*` (runtime-context); prompt/response names (`ai.prompt.messages`/`ai.response.text`/`toolCalls`) unchanged. Updated `tracePromotion.ts` to read v7 names (v6 kept as fallback) + added a v7 unit case (11/11 pass). Re-ran the live smoke: the trace now promotes correctly — `name: agent-turn`, `sessionId: <thread>`, `userId: <name>`. The "named and grouped by thread session" requirement holds under v7. (Note: before the fix, traces were unnamed `invoke_agent …` with null session/user.)

## 5. Langfuse trace cleanup (D4 — source-confirmed the duplication PERSISTS in v7)

- [~] 5.1 Drive a live turn post-migration and INSPECT the trace (`langfuse-cli`): count `send_message`/`ai.streamText` spans vs runtime sends/steps to CONFIRM the source prediction. **BLOCKED by a NEW finding — durable turns emit ZERO telemetry under v7.** Drove a real-model durable turn live (sunny on v7 via devbox; loopback channel): the turn ran + delivered a real reply (run `completed`, message sent) — but NO Langfuse trace was produced (confirmed over multiple flush windows; only the standalone main-process smokes appear). Root cause: v7 moved telemetry from the OTel-global tracer to `ai`'s `registerTelemetry` registry, which is REALM-LOCAL; the WDK runs the durable orchestration in an isolated VM (own `globalThis` — confirmed by `@workflow/core` class-serialization's own comment) and steps in separate inlined-`ai` bundles, so `registerTelemetry` (called in the main process by `instrumentation.ts`) never reaches the `WorkflowAgent`. A per-call `telemetry.integrations: [new OpenTelemetry()]` override (tried + reverted) didn't help — the integration's `trace.getTracer('ai')` resolves to a no-op in a realm with no registered provider. So the spans are silently non-recording. v6 worked because it emitted via the OTel global API. **This blocks 5.1/5.4 and is an observability REGRESSION (durable traces vanished, not just duplicated).** Fix directions for a focused follow-up: register the OTel provider/integration INSIDE the step-bundle realm (steps run in real Node; `@opentelemetry/api`'s provider is process-global, so the `doStream` generation span could emit from a step) and/or inside the workflow VM for the agent-loop spans; or report upstream that `registerTelemetry` doesn't propagate to WDK workflow realms (relates to 5.3). The CORE migration is unaffected — durable turns work functionally (this is the **8.1 loopback live smoke, default-durable + real model: PASSING**).
- [x] 5.2 ~~Add an OTel SpanProcessor/Sampler that drops replay-context spans~~ **SUPERSEDED by the realm finding + decision (ship with durable telemetry OFF).** The replay-duplication problem is moot because durable turns emit NO external spans at all (the `node:vm` realm gap, not just duplication). A 3-fork race settled the fix space: Option B (register in step realm) is impossible (source-proven: all AI-SDK telemetry dispatches from the agent loop in the VM); Option A (a per-call custom `Telemetry` integration that forwards lifecycle events out of the VM via a journaled `'use step'`) WORKS — proven offline with an in-memory exporter capturing the full hierarchy + session attrs, and journaling gives replay-dedup for free — but is **shelved** (negative ROI for a lightly-used 1-user Langfuse). DECISION: durable telemetry is **explicitly disabled** (`telemetry.isEnabled: false` in all 3 workflows, with rationale); re-enable by pulling the shelf branch when Langfuse use grows or upstream fixes it. Shelf: git branch `worktree-agent-af47988b13eeb3162` (commit `fb59d6c`).
- [ ] 5.3 Report the gap upstream on `vercel/ai` #12164 ("Make AI SDK work in Workflow", OPEN). **Reframed by research (2026-06-28):** the more fundamental gap is that `registerTelemetry(new OpenTelemetry())` (and Langfuse's `LangfuseVercelAiSdkIntegration`) are global/realm-LOCAL and silently no-op inside the WDK workflow VM — there is NO documented OTel/Langfuse path for WorkflowAgent (only a DevTools/custom-store example via per-call `telemetry.integrations` + a `'use step'`/`workflow.fetch` bridge). File: "Provide a realm-bridging OTel integration (or docs) for WorkflowAgent; `registerTelemetry(new OpenTelemetry())` produces no spans for durable agents." Telemetry impl landed in #15074/PR#15078 (2026-05-08); #12164 still carries an open telemetry `TODO`. (The earlier replay-duplication framing is secondary and is naturally fixed by the journaled-step bridge — see 5.2.)
- [x] 5.4 ~~Confirm "one trace per turn, no replay duplication" holds~~ **Requirement AMENDED, not met as originally written.** Durable external telemetry is off (above), so there is no external trace to be clean. The delta spec (`specs/durable-execution/spec.md`) was rewritten: turns remain observable via the WDK workflow runs inspector; the external-trace-cleanliness guarantee is DEFERRED (vercel/ai#12164) and durable telemetry is explicitly disabled rather than silently failing. File upstream: see 5.3 (draft in `UPSTREAM-12164.md`).

## 6. Client streaming — evaluate retiring the LiveBus chunk-bridge (D3)

- [x] 6.1 / 6.2 **DEFERRED — LiveBus chunk-bridge KEPT (no change).** The dashboard streaming path works unchanged: the reader-side `createModelCallToUIChunkTransform()` (added in 3.1, in `durableRouter.ts` + the dashboard SSE route) adapts the v7 `ModelCallStreamPart` run stream to the existing `UIMessageChunk` consumers, so the LiveBus + thread→active-run tracking carry over as-is. Retiring the bridge via `WorkflowChatTransport` is an optional future optimization, not needed for the migration; not pursued.

## 7. Verify behavioral parity (D2 unknowns)

- [x] 7.1 Reasoning/thinking: the reasoning strip is KEPT unchanged — `assistantUIMessageFromResponse` (D-MG9 rebuild) drops `reasoning` parts before persisting, and `toModelMessages`/`stripReasoning` drops them from history, so re-sent Anthropic thinking blocks remain impossible. A live durable turn ran clean (no Anthropic thinking-block rejection). Kept conservative; no v7 reasoning primitive adopted.
- [x] 7.2 Steering: `loadSteers` (store-read in `prepareStep`) folds mid-turn messages on `WorkflowAgent` — verified by the workflow suite's "folds a message that arrives mid-turn" test (green). Kept; no v7 message-queue primitive adopted (none proven safe vs the FIFO parking hazard).
- [x] 7.3 Exactly-once on replay + per-thread serialization + `processedAt` watermark all hold — covered by the workflow suite (exactly-once-on-replay test green; the serializable mock crosses the journaled step boundary) + the in-process unit/integration suites (257 + 29 green).

## 8. Cutover

- [x] 8.1 Full verification: typecheck 0; unit 257; integration 29; `@workflow/vitest` workflow suite 4/4; production `nitro build` succeeds; **loopback live smoke PASSED** (default-durable + real model — a real durable `WorkflowAgent` turn delivered a reply in prod). Eval lane not run (real-model cost; harness compiles). Langfuse "named/grouped/clean" intentionally N/A — durable telemetry off (5.x).
- [~] 8.2 Deploy + docs + PR + archive. **Prod runs v7** (sunny devbox on this worktree; durable turns are the default — the `SUNNY_DURABLE_TURNS` gate no longer exists in code). Rollback = revert the change (git). AGENTS.md/README + memory updated for the v7 stack + the telemetry-off decision. Archiving the change now; PR is the user's call.
